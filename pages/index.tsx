import React from "react";
import Header from "../components/Header";
import GroundTopPart from "../components/GroundTopPart";

interface indexProps {}

const Index: React.FunctionComponent<indexProps> = ({}) => {
  return <Header />;
};

export default Index;
